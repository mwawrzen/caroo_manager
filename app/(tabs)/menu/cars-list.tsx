import { ThemedText } from "@/components/themed/themed-text";
import { ThemedView } from "@/components/themed/themed-view";

export default function CarsList() {
  return (
    <ThemedView>
      <ThemedText>cars list.</ThemedText>
    </ThemedView>
  );
};
